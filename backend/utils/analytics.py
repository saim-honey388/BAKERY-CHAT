"""Analytics utilities (minimal)."""

def track(event_name: str, payload: dict = None):
    # Stub: print to stdout or integrate with real analytics later
    print(f"ANALYTICS: {event_name} - {payload}")
